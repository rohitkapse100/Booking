import { LightningElement } from 'lwc';
import static_images from '@salesforce/resourceUrl/static_images';
export default class studentLogo extends LightningElement{
    imageUrl = static_images + '/images/c/studentLogo';
}